using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

abstract class MenuScreen {
    /*
    private float deadZone = 3.0f;
    protected float GetAnyAxis(string axisName) {
        float rAxis = 0;
        for (int i = 0; i < 9; i++) {
            if (i == 0) { // Keyboard

            }
            else {

            }
            if (rAxis > deadZone || rAxis < -deadZone) {
                break;
            }
        }
        return rAxis;
    }

    protected int? GetAnyAccept() {

    }

    protected int? GetAnyBack() {

    }
    */
}
